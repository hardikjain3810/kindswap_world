apiVersion: v1
kind: Service
metadata:
  name: kindswap-frontend
  namespace: {{ .Release.Namespace }}
  labels:
    app: kindswap-frontend
spec:
  selector:
    app: kindswap-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  type: ClusterIP
